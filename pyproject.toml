[project]
name = "urban-green"
version = "1.0.0"
description = "Application for Urban Green Farms"
requires-python = ">=3.10.0"

dependencies = [
    "apache-airflow-providers-postgres",
    "apache-airflow-providers-amazon",
    "pyarrow",
    "pandas",
]