-- Urban Green Analytics warehouse database.
-- Keep CLICKHOUSE_DB=urbangreen in .env so Docker configuration and
-- fully-qualified table names remain aligned.

CREATE DATABASE IF NOT EXISTS urbangreen;
